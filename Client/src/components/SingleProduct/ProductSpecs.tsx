import { TProduct } from "../../types/products";

type Params = {
    product: TProduct;
  };

const ProductSpecs = ({product}:Params) => {
  return (
    <div>
        
    </div>
  )
}

export default ProductSpecs